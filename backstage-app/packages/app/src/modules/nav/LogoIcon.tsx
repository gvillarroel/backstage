export const LogoIcon = () => {
  return (
    <div
      style={{
        width: 28,
        height: 28,
        borderRadius: 0,
        background: 'var(--fdx-brand-red)',
        color: 'var(--fdx-surface)',
        display: 'grid',
        placeItems: 'center',
        fontSize: 14,
        fontWeight: 800,
      }}
    >
      AI
    </div>
  );
};
